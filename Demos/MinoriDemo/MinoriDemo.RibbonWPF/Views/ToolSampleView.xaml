<views:MvxWpfView
    x:Class="MinoriDemo.RibbonWPF.Views.ToolSampleView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:MinoriDemo.RibbonWPF.Views"
    xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
    xmlns:picker="clr-namespace:ColorPickerLib.Controls;assembly=ColorPickerLib"
    mc:Ignorable="d" 
    d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Background="LightBlue">
        <StackPanel Orientation="Horizontal">
            <TextBlock x:Name="FileName" />
            <Button x:Name="Export" Content="Export" Width="50" Height="30" />
        </StackPanel>
        <ListView x:Name="MainResourceList">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <picker:ColorPicker Width="50" Height="25" SelectedColor="{Binding Color}" />
                        <TextBlock Text="{Binding Key}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</views:MvxWpfView>
